<%= form_for @player, :html => { :class => "form-horizontal player" } do |f| %>

  <% if @player.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@player.errors.count, "error") %> prohibited this player from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @player.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>


  <div class="form-group">
    <%= f.label :playerName, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :playerName, :class => 'form-control' %>
    </div>
    <%=f.error_span(:playerName) %>
  </div>

  <div class="form-group">
    <%= f.label :ExpertIn, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :ExpertIn, :class => 'form-control' %>
    </div>
    <%=f.error_span(:ExpertIn) %>
  </div>

  <div class="form-group">
    <%= f.label :Team_id, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :Team_id, :class => 'form-control' %>
    </div>
    <%=f.error_span(:Team_id) %>
  </div>


  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                players_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
